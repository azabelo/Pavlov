# Agents

## Cursor Cloud specific instructions

This repository ("Pavlov") is currently an empty project with only a `README.md` placeholder. There are no services, dependencies, build systems, or tests to run.

### Current state
- No package manager or dependency files exist
- No source code, tests, or build configuration
- No services to start or ports to expose

### For future agents
- Once code is added, update this section with relevant dev environment setup notes, lint/test/build/run commands, and any non-obvious caveats
- Update the VM environment script (via `SetupVmEnvironment`) when dependencies are introduced

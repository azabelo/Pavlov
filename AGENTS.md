# Pavlov

## Cursor Cloud specific instructions

This repository is currently an empty scaffold (initial commit with only `README.md`). There are no services, dependencies, build steps, lint checks, or tests to run.

When application code is added in the future, update this section with:
- How to install dependencies
- How to run lint, tests, and the dev server
- Any non-obvious caveats discovered during development
